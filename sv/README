Determines sequences of inserts detected by sniffles

To run:

cd pitchfork
make init
make blasr
cd ../
./go.sh


Dependencies:

samtools
python 2


Files needed (currently set as variables in go.sh):

vcfFile (output from sniffles)
bamFile (all reads)
fastaFile (reference genome)

Output:
Folder seqs with files <pos>.insert containing insert sequence at position <pos>
